# Defaults del entorno dev (se versiona). NO poner secretos aquí:
# .env.$APP_ENV se carga DESPUÉS de .env.local y lo sobreescribiría.
# El APP_SECRET de desarrollo vive en .env.local (no versionado).
